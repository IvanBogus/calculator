Additional information
----------------------

This project contains a simple C++ calculator implementation.
The calculator supports addition, subtraction, and multiplication
with rounding behavior adjusted for floating-point inputs.

To compile the project manually, use:

    g++ -std=c++11 calculator.cpp main.cpp -o calculator

Run the produced executable to test basic functionality.
